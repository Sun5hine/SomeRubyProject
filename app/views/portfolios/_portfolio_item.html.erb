<p><%=link_to portfolio_item.title, portfolio_show_path(portfolio_item) %> </p>
<p><%=portfolio_item.subtitle%> </p>
<p><%=portfolio_item.body%> </p>
<%=image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %><br>
<%= link_to "Edit the Portfolio item", edit_portfolio_path(portfolio_item)%> <br>

<%= link_to "Delete the Portfolio item", portfolio_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?' } %>
